在 VS Code 中，无论你当前在**编辑器**还是**终端**，都可以用快捷键快速切换，完全不需要用鼠标。

## 核心快捷键

| 你想跳到哪里 | 快捷键 (Windows) | 说明 |
|-------------|-----------------|------|
| **编辑器 → 终端** | `` Ctrl + ` `` | 反引号键（Esc 键下面那个） |
| **终端 → 编辑器** | `` Ctrl + ` `` | **同一个快捷键**，来回切换 |
| **编辑器 → 终端** | `Ctrl + J` | 打开/关闭面板（终端在面板里） |

> 💡 `` Ctrl + ` `` 是最常用的，按一下跳到终端，再按一下跳回编辑器。

## 更多切换方式

| 目标位置 | 快捷键 | 说明 |
|----------|--------|------|
| 编辑器区域 | `Ctrl + 1` | 跳转到第 1 个编辑器组 |
| 编辑器区域 | `Ctrl + Tab` | 循环切换打开的文件 |
| 终端面板 | `` Ctrl + ` `` | 切换焦点到终端 |
| 侧边栏（文件列表） | `Ctrl + Shift + E` | 跳到资源管理器 |
| 任意面板之间 | `Ctrl + Tab` | 循环切换焦点 |

## 实际使用场景

### 场景一：在编辑代码，想临时去终端输命令
1. 直接按 `` Ctrl + ` `` → 焦点跳到终端
2. 输入命令，按回车执行
3. 再按 `` Ctrl + ` `` → 回到编辑器继续写代码

### 场景二：终端输完命令，想回到刚才编辑的位置
同样，按 `` Ctrl + ` `` 就回来了，光标还在你之前编辑的那一行。

### 场景三：想把终端面板变大/变小
| 操作 | 快捷键 |
|------|--------|
| 增大终端高度 | `Ctrl + Shift + [` 或 `Ctrl + Shift + ]` |
| 打开新终端 | `Ctrl + Shift + `` ` |
| 关闭终端 | 在终端中输入 `exit` 或按 `Ctrl + C` |

## 如果快捷键没反应

检查一下：
1. **键盘布局**：确认你的 ` `` ` 键在 Esc 下面、数字 1 左边
2. **终端是否隐藏**：按 `Ctrl + J` 先打开面板，再按 `` Ctrl + ` `` 切换焦点
3. **快捷键冲突**：检查有没有其他软件占用了这个快捷键（比如输入法）

## 记忆技巧

- `` Ctrl + ` `` = 终端（Terminal）的 ` 符号长得像一个小终端
- `Ctrl + 1` = 回到第 1 个编辑窗口（主要工作区）
- `Ctrl + J` = 面板（Panel），终端就在面板里